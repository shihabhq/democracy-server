Certificate template
====================
Copy the certificate image here so the server can generate PDFs:

  From: democracy-client/public/certificate/image.png
  To:   democracy-server/assets/certificate/image.png

If the image is missing, the server will try the client path when run from the monorepo root.
